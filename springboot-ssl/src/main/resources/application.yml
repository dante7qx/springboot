server:
  port: 8443
  ssl:
    key-store: classpath:server.jks
    key-store-password: 1234@@qwer
    key-password: 12,qw,as
    key-alias: SpiritKey
    enabled: true
  http2:
    enabled: true # 启用 HTTP/2，必须配置 HTTPS