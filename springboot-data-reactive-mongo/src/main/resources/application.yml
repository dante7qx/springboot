## If you use the Mongo 3.0 Java driver, spring.data.mongodb.host and spring.data.mongodb.port are not supported. 
## In such cases, spring.data.mongodb.uri should be used to provide all of the configuration.

spring:
  data:
    mongodb:
      uri: mongodb://dante:123456@localhost:27017/reactive_mongo
      authentication-database: admin    
logging:
  level:
    root: INFO
    org.springframework.data.mongodb: DEBUG

## 现在执行 mongo 命令    
## use reactive_mongo
## db.createUser({user:"dante",pwd:"123456",roles:[{role:"readWrite",db:"reactive_mongo"}],mechanisms:["SCRAM-SHA-1"]})